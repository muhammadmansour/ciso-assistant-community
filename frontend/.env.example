# Copy to frontend/.env and adjust for your environment.
# Vite dev loads this automatically; PM2 reads it via start-pm2.sh.

# GRC Admin (sidebar + legislative updates sign-in link)
PUBLIC_WATHBAH_ADMIN_CONSOLE_URL=https://grc-admin.wathbah.dev/

# Muraji (frontend server routes)
MURAJI_API_BASE_URL=https://muraji-api.wathbah.dev
