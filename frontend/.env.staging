# Used by: pnpm run build:staging
# Browser → same-origin API on grc-stage; SSR → local Django on staging port 8020.
PUBLIC_BACKEND_API_URL=http://127.0.0.1:8020/api
PUBLIC_BACKEND_API_EXPOSED_URL=https://grc-hrsd.wathbahs.com/api

# External services (override per environment)
# PUBLIC_WATHBAH_ADMIN_CONSOLE_URL=https://grc-admin-stage.wathbahs.com/
# MURAJI_API_BASE_URL=https://muraji-stage.wathbahs.com
# LEGISLATIVE_UPDATES_API_URL=https://grc-admin-stage.wathbahs.com/api/ai-tools/pipeline-legislative-updates
