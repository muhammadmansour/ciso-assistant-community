"""OAuth 2.1 bridge that lets remote MCP clients (e.g. Claude "custom connectors")
authorize against CISO Assistant using their normal email/password login,
instead of a static Personal Access Token in a local config file.
"""
