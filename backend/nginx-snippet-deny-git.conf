# Include inside server { } — not valid under http { }.
# Blocks /.git/* scanner traffic at nginx (avoids proxying to Node). Does not touch /.well-known/.

location ~ ^/\.git {
    deny all;
    access_log off;
    log_not_found off;
    return 404;
}
